{
    /* 
     * Default: net.roguelogix.biggerreactors.Config$CyaniteReprocessor@358f0b13
     */
    CyaniteReprocessor: {
        /* 
         * Max water capacity
         * Valid range: (0,)
         * Default: 5000
         */
        WaterTankCapacity: 5000.0,

        /* 
         * Time (in ticks) it takes to complete a job.
         * Valid range: (0,)
         * Default: 200
         */
        TotalWorkTime: 200.0,

        /* 
         * Max energy capacity.
         * Valid range: (0,)
         * Default: 5000
         */
        EnergyTankCapacity: 5000.0,

        /* 
         * Power usage per tick of work.
         * Valid range: (0,)
         * Default: 1
         */
        EnergyConsumptionPerTick: 1.0,

        /* 
         * Max transfer rate of fluids and energy.
         * Valid range: (0,)
         * Default: 500
         */
        TransferRate: 500.0,

        /* 
         * Water usage per tick of work.
         * Valid range: (0,)
         * Default: 1
         */
        WaterConsumptionPerTick: 1.0
    },

    /* 
     * Default: net.roguelogix.biggerreactors.Config$HeatExchanger@33437d56
     */
    HeatExchanger: {
        /* 
         * Valid range: [3,)
         * Default: 64
         */
        MaxWidth: 64.0,

        /* 
         * Default: net.roguelogix.biggerreactors.Config$Reactor$GUI@38ad4b3c
         */
        gui: {
            /* 
             * Default: 2000
             */
            HeatDisplayMax: 2000.0
        },

        /* 
         * Valid range: [4,)
         * Default: 96
         */
        MaxHeight: 96.0,

        /* 
         * Valid range: [3,)
         * Default: 64
         */
        MaxLength: 64.0
    },

    /* 
     * Default: net.roguelogix.biggerreactors.Config$Turbine@412c63c0
     */
    Turbine: {
        /* 
         * Valid range: [5,)
         * Default: 32
         */
        MaxWidth: 32.0,

        /* 
         * Valid range: [4,)
         * Default: 192
         */
        MaxHeight: 192.0,

        /* 
         * Valid range: [5,)
         * Default: 32
         */
        MaxLength: 32.0
    },

    /* 
     * Default: net.roguelogix.biggerreactors.Config$WorldGen@604e9d3e
     */
    WorldGen: {
        /* 
         * Valid range: [1,)
         * Default: 5
         */
        YelloriteOreMaxClustersPerChunk: 5.0,

        /* 
         * Default: true
         */
        EnableYelloriteGeneration: false,

        /* 
         * Valid range: [5,)
         * Default: 50
         */
        YelloriteOreMaxSpawnY: 50.0,

        /* 
         * Valid range: [1,)
         * Default: 10
         */
        YelloriteMaxOrePerCluster: 10.0
    },

    /* 
     * Default: net.roguelogix.biggerreactors.Config$Reactor@2fc4b8bb
     */
    Reactor: {
        /* 
         * Valid range: [3,)
         * Default: 128
         */
        MaxLength: 128.0,

        /* 
         * Valid range: (0,)
         * Default: 1.0
         */
        FuelUsageMultiplier: 1.0,

        /* 
         * Valid range: [3,)
         * Default: 128
         */
        MaxWidth: 128.0,

        /* 
         * Default: net.roguelogix.biggerreactors.Config$Reactor$GUI@62edf852
         */
        GUI: {
            /* 
             * Default: 2000
             */
            HeatDisplayMax: 2000.0
        },

        /* 
         * Valid range: (0,)
         * Default: 1.0
         */
        OutputMultiplier: 1.0,

        /* 
         * Valid range: [3,)
         * Default: 192
         */
        MaxHeight: 192.0
    }
}